<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Wildberries API connection
    | В .env.example указан официальный хост statistics-api.wildberries.ru
    | В локальном .env можно переопределить на кастомный прокси
    |--------------------------------------------------------------------------
    */
    'host'      => env('WB_HOST', 'statistics-api.wildberries.ru'),
    'key'       => env('WB_KEY', ''),

    /*
    |--------------------------------------------------------------------------
    | Default date range for fetching data
    |--------------------------------------------------------------------------
    */
    'date_from' => env('WB_DATE_FROM', '2024-01-01'),
    'date_to'   => env('WB_DATE_TO', date('Y-m-d')),

    /*
    |--------------------------------------------------------------------------
    | Items per page (max 500 per WB docs)
    |--------------------------------------------------------------------------
    */
    'limit'     => (int) env('WB_LIMIT', 500),

    /*
    |--------------------------------------------------------------------------
    | Retry settings for "Too many requests" (HTTP 429)
    |--------------------------------------------------------------------------
    */
    'retry_times'   => (int) env('WB_RETRY_TIMES', 3),
    'retry_sleep'   => (int) env('WB_RETRY_SLEEP', 5),  // seconds (exponential: 5, 15, 45)
];
